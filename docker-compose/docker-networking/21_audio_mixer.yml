# Norsk Media example: 21_audio_mixer
#
# Generate an audio tone and publish it in an audio-only HLS playlist
#

version: "3.5"

networks:
  norskNetwork:
    name: norsk-nw

services:
  norsk:
    container_name: norsk-server
    image: norskvideo/norsk:v1.0.336-main
    ports:
      - 1935:1935/tcp # For RTMP sources
      - 5001:5001/udp # For SRT sources
      - 5002:5002/udp # For SRT sources
      - 6790:6790/tcp # GRPC interface
      - 6791:6791/tcp # Debugging endpoints, including the workflow visualizer, logs, and docs
      - 8080:8080/tcp # Public endpoints for HLS/DASH and WebRTC players
    user: "${DOCKER_UID}:${DOCKER_GID}"
    volumes:
      - ${LICENSE_FILE}:/mnt/license.json:ro
      - ./data:/mnt/data
      - ${LOG_ROOT}:/var/log/norsk
      
    command: --license-file /mnt/license.json 
    shm_size: '2gb'
    # cpuset: "0-15"
    networks:
      - norskNetwork
  example:
    container_name: norsk-example-app
    ports:
      - 3000:3000/tcp # Some sources have an internal web UI, published on 3000
    build:
      context: examples
      dockerfile: Dockerfile
      network: host # note this is for build only, not at runtime. Without it npm install can fail on some systems
    user: "${DOCKER_UID}:${DOCKER_GID}"
    environment:
      - NORSK_HOST=norsk
      - PUBLIC_URL_PREFIX=${PUBLIC_URL_PREFIX}
      - DEBUG_URL_PREFIX=${DEBUG_URL_PREFIX}
      - CLIENT_HOST_EXTERNAL=${CLIENT_HOST}
      - CLIENT_HOST_INTERNAL=norsk-example-app
      
    depends_on:
      norsk:
        condition: service_healthy
    command: node -e 'require("./lib/src/21_audio_mixer").main()'
    networks:
      - norskNetwork
  # [Start of source]
  source_0:
    container_name: norsk-source_0
    image: datarhei/ffmpeg
    entrypoint: ["/bin/sh","-c"]
    user: "${DOCKER_UID}:${DOCKER_GID}"
    volumes:
      - ./data:/tmp/data:ro
    command:
    - |
        sleep 1
        ffmpeg -v error -re -stream_loop -1 -i data/Weaving-stereo.ts -vcodec copy -acodec copy -f flv 'rtmp://norsk:5001/norsk/source1'
    depends_on:
      - example
    networks:
      - norskNetwork
  # [End of source]
  # [Start of source]
  source_1:
    container_name: norsk-source_1
    image: datarhei/ffmpeg
    entrypoint: ["/bin/sh","-c"]
    user: "${DOCKER_UID}:${DOCKER_GID}"
    volumes:
      - ./data:/tmp/data:ro
    command:
    - |
        sleep 1
        ffmpeg -v error -re -stream_loop -1 -i data/Weaving-stereo.ts -vcodec copy -acodec copy -f flv 'rtmp://norsk:5002/norsk/source2'
    depends_on:
      - example
    networks:
      - norskNetwork
  # [End of source]
  # [Start of source]
  source_2:
    container_name: norsk-source_2
    image: datarhei/ffmpeg
    entrypoint: ["/bin/sh","-c"]
    user: "${DOCKER_UID}:${DOCKER_GID}"
    volumes:
      - ./data:/tmp/data:ro
    command:
    - |
        sleep 1
        ffmpeg -v error -re -stream_loop -1 -i data/InkDrop-stereo.ts -vcodec copy -acodec copy -f flv 'rtmp://norsk:5003/norsk/source3'
    depends_on:
      - example
    networks:
      - norskNetwork
  # [End of source]
  # [Start of source]
  source_3:
    container_name: norsk-source_3
    image: datarhei/ffmpeg
    entrypoint: ["/bin/sh","-c"]
    user: "${DOCKER_UID}:${DOCKER_GID}"
    volumes:
      - ./data:/tmp/data:ro
    command:
    - |
        sleep 1
        ffmpeg -v error -re -stream_loop -1 -i data/InkDrop-stereo.ts -vcodec copy -acodec copy -f flv 'rtmp://norsk:5004/norsk/source4'
    depends_on:
      - example
    networks:
      - norskNetwork
  # [End of source]
